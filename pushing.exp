git add .

echo 'Enter the commit message:'
read commitMessage

git commit -m "$commitMessage"

echo 'Enter the name of the branch:'
read branch

git push origin $branch

match_max 100000
expect -exact "git@abijithksym's password: "
send -- "symptots123\r"
